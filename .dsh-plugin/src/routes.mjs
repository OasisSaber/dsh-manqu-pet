// 路由前缀单一来源（client 与 Node half 共用；改前缀只改这里）。
export const ROUTE_PREFIX = '/manqu'
export const STATE_PATH = `${ROUTE_PREFIX}/state`
export const ASSETS_PATH = `${ROUTE_PREFIX}/assets`
export const EVENTS_PATH = `${ROUTE_PREFIX}/events`
